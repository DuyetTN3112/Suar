import type {
  SliceItem as GeneratedSliceItem,
  SliceMetadata as GeneratedSliceMetadata,
} from '@/apps/user/shared/generated/data'

export type SliceItem = GeneratedSliceItem
export type SliceMetadata = GeneratedSliceMetadata
