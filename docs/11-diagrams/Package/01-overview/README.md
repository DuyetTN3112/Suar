# Package Overview

```text
01-overview/
├── overview/    # Bản đồ package toàn hệ thống
├── high-level/  # Chưa cần: chi tiết nằm ở domain kế tiếp
└── low-level/   # Chưa cần
```

`pkg_01_overview` là điểm bắt đầu; tiếp tục sang `02`–`05` theo concern.
