# System Context Rich Pictures

- Overview: stakeholders and system boundary.
- High level: problems and responses.
- Low level: report-readable stakeholder and problem-response slices, plus architecture constraints and operational concerns.

Đọc từ người/boundary tới vấn đề, sau đó mới xuống constraint kỹ thuật.

`rp_01a`/`rp_01b` tách overview stakeholder; `rp_02a`/`rp_02b` tách problem-response narrative. Các file này chỉ là derived report slices để đọc trên A4, không tạo capability hoặc problem situation mới. Không tạo thêm implementation inventory ở family này; package, class, deployment và interaction detail nằm trong family tương ứng.
