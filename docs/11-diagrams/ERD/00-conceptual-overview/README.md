# Conceptual ERD

```text
00-conceptual-overview/
├── overview/    # Bảy business anchors toàn hệ thống
├── high-level/  # Logical detail nằm trong domain 01–05
└── low-level/   # Physical inventory nằm trong domain 01–04
```

`conceptual_erd_domain` là entry point duy nhất; không thêm field hoặc join-table detail vào đây.
