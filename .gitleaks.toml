title = "Suar secret scanning configuration"

[extend]
useDefault = true

[allowlist]
description = "Public, fixed CI-only application key fixture"
regexTarget = "match"
regexes = [
  '''test-key-at-least-16-chars-long''',
]
