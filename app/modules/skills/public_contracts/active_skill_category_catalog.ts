/**
 * Persistence-agnostic identity returned to consumers that need to filter their
 * own records by Skills-owned category and activation policy.
 */
export interface ActiveSkillIdFact {
  id: string
}
